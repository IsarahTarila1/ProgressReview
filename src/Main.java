import Models.Contact;
import Utils.ContactManager;

public class Main {
    public static void main(String[] args) {
        ContactManager contactManager = new ContactManager();
        contactManager.executeActivity();
    }
}